package application;

class Count_item {
  	 String day;
  	 int line;
  	 int cut;
  	 
  	Count_item(String day, int line, int cut) {
  		this.day = day;
  		this.line = line;
  		this.cut = cut;
  	     }
  	 }
   